import Flash, {
  MessageOptions as MOptions,
  hideMessage as hide,
  showMessage as show,
} from 'react-native-flash-message'

export const FlashMessage = Flash
export const hideMessage = hide
export const showMessage = show
export type MessageOptions = MOptions
